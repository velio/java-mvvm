import org.gradle.api.artifacts.*

apply plugin: 'java' // To add "clean" task to the root project.

repositories {
    mavenCentral()
}

dependencies {
    compile 'javax.ejb:javax.ejb-api:3.2'
    compile 'javax:javaee-api:7.0'
}